"use strict";
var BEngine;
(function (BEngine) {
    var grids;
    (function (grids) {
        var ElementGrid = /** @class */ (function () {
            function ElementGrid() {
            }
            return ElementGrid;
        }());
        grids.ElementGrid = ElementGrid;
    })(grids = BEngine.grids || (BEngine.grids = {}));
})(BEngine || (BEngine = {}));
